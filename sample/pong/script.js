const canvas = document.getElementById('gameCanvas'); const ctx = canvas.getContext('2d'); const scoreElement = document.getElementById('score'); let playerScore = 0; let cpuScore = 0; const paddleWidth = 10, paddleHeight = 100, ballSize = 10; let playerY = 250, cpuY = 250, ballX = 400, ballY = 300, ballDX = 5, ballDY = 5; let upPressed = false, downPressed = false; document.addEventListener('keydown', (e) => { if (e.key === 'ArrowUp') upPressed = true; if (e.key === 'ArrowDown') downPressed = true; }); document.addEventListener('keyup', (e) => { if (e.key === 'ArrowUp') upPressed = false; if (e.key === 'ArrowDown') downPressed = false; }); function update() { if (upPressed && playerY > 0) playerY -= 7; if (downPressed && playerY < canvas.height - paddleHeight) playerY += 7; cpuY += (ballY - (cpuY + paddleHeight / 2)) * 0.1; ballX += ballDX; ballY += ballDY; if (ballY <= 0 || ballY >= canvas.height - ballSize) ballDY *= -1; if (ballX <= paddleWidth && ballY > playerY && ballY < playerY + paddleHeight) ballDX *= -1; if (ballX >= canvas.width - paddleWidth - ballSize && ballY > cpuY && ballY < cpuY + paddleHeight) ballDX *= -1; if (ballX < 0) { cpuScore++; ballX = 400; ballY = 300; } if (ballX > canvas.width) { playerScore++; ballX = 400; ballY = 300; } scoreElement.innerText = `Player: ${playerScore} | CPU: ${cpuScore}`; } function draw() { ctx.fillStyle = 'black'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.fillStyle = 'white'; ctx.fillRect(0, playerY, paddleWidth, paddleHeight); ctx.fillRect(canvas.width - paddleWidth, cpuY, paddleWidth, paddleHeight); ctx.fillRect(ballX, ballY, ballSize, ballSize); } function loop() { update(); draw(); requestAnimationFrame(loop); } loop();